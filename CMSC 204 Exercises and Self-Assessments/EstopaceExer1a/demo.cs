/*

4. create unique pages per options in the program
5. create a persisting options menu by using a while loop
 */
int maxcount = 1000;
string[] newarray = new string[maxcount];

int count = -1; // so that the initial value of the index of the stack is 0 once we add 1 to this. 